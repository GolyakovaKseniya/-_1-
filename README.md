# Задача 1
#### Код был разделен на модули: Header.h, Source.cpp, и main.cpp
#### Был использован using namespace std, заменено на <std::>
#### Простая структура имела неправильное название Artifact, она должна иметь в названии перфикс F
#### В структуре имена публичных полей начинались с маленькой буквы, исправлено на заглавные
#### Также указан неточный размер int, исправлено на int32
#### Название логической функции SortWeight не соответствует код-стайлу, изменено на IsLighterThan (теперь она как бы задает вопрос), также непонятное название переменных (a, b) , изменено на const FArtifact& First, const FArtifact& Second
#### У этой функции неправильно стояла фигурная скобка, рядом с ней, а не на следующей строке
#### Из функции main убран алгоритм для поиска оптимального набора артефактов в целях оптимизации
#### Изначальный код не выводил номера выбранных артефактов, поэтому в алгоритм выше добавлен вектор для хранения текущих и итоговых индексов, и в цикл добавлена работа с ним
#### Исправлено setlocale(0, "") на setlocale(LC_ALL, "")
#### Исправлено непонятное название переменной для работы с файлом с f1 на InputFile
#### Исправлено непонятное название переменной для работы с весом и объемом (n, b, sumw, sumv, k, maxw, maxv ->  ArtifactCount, MaxTotalWeight, CurrentWeight, CurrentVolume, StartIndex, BestWeight, BestVolume)
#### Добавлены проверки на корректность считывания из файла чисел веса, объема и тд
#### Исправлено постфиксное i++ на префиксное
#### Добавлен вывод номеров артефактов

# Задача 2
#### Код был разделен на модули: Header.h, Source.cpp, и main.cpp
#### Был использован using namespace std, заменено на <std::>
#### Функция чтения из файла имеет непонятное название readfile, исправлено на ReadBoardFromFile, также непонятное название переменных filename, line, cols. Также функция читает два числа, а необходимо одно, поэтому передаем в функция только 2 параметра: const std::string& Filename, int32_t& BoardSize
#### У этой функции неправильно стояла фигурная скобка, рядом с ней, а не на следующей строке
#### Исправлено непонятное название переменной для работы с файлом с input на InputFile
#### В цикле if использовано грубое exit(1), исправлено на return{}
#### Добавлены проверки на изначальное условие размера доски, корректность чтения, ввода числа в файле
#### Функция ввода с клавиатуры имеет непонятное название readkey, исправлено на  ReadBoardFromKeyboard, также читает два числа вместо одного, теперь передаем в нее только 1 число.
#### В ней также нет проверки на размер доски, корректность ввода
#### Функция поиска максимума также имеет непонятное название MaxSum исправлено на CalculateMaxSum
#### Исправлено непонятное название переменной для доски с dp на DynamicTable
#### Исправлено постфиксное j-- на префиксное
#### В main нет проверки на ввод целых чисел и тд, также используется две перменные вместо одной
#### Неправильный ввод в файл

# Задача 3
#### Код был разделен на модули: Header.h, Source.cpp, и main.cpp
#### Был использован using namespace std, заменено на <std::>
#### Убраны лишние пробелы
#### Так как весь код находился в функции main разделим его по составляющим и распределим по функциям
#### Была ошибка в проверке начальных условий, вместо digitCount <= 1 было < 1
#### Исправлено непонятное название переменных n, k на DigitCount, NumberBase
#### Не было проверок на корректность ввода для этих чисел, добавлено в отдельную функцию
#### Также отсутствовало форматирование вывода вещественных чисел 
#### Исправлено непонятное название переменных nz, _nz, oz, _oz, tz, _tz на ValidNoEndZero, ValidEndZero, InvalidWithZeros, PreviousNoEndZero, PreviousEndZero, PreviousInvalid
#### Была ошибка в начальных условиях, validEndZero = 1 а должно быть 0
#### В цикле for было использование переменной с плавающей точкой в качестве счетчика, исправлено на int
#### Исправлено постфиксное i++ на префиксное
#### Исправлено непонятное название переменной result на  TotalValidNumbers

# Задача 4
#### Код был разделен на модули: Header.h, Source.cpp, и main.cpp
#### Был использован using namespace std, заменено на <std::>
#### Изменено названии функции calc, так как оно не информативное, на  CalculateExpression, и ее парамерт с str на Expression
#### Изменено названии функции element, так как оно не информативное, на ParseElement, и ее парамерт с str на Expression
#### Изменено названии функции term, так как оно не информативное, на ParseTerm, и ее парамерт с str на Expression
#### В функции ParseElement была проведена работа с индексами без проверки границ
#### Также не было проверок на корректность ввода, было использование stoi без обработки исключений
#### В функции ParseTerm Исправлено непонятное название переменной pos на MultiplicationPos, level на Level
#### Исправлено постфиксное i++ на префиксное
#### В функции был проведен поиск операции умножения без учета приоритета скобок
#### Цикл if несколько раз был использован без фигурных скобок
#### В функции CalculateExpression Исправлено непонятное/неправильное название переменных level на Level, pos на OperationPos, ch на Operation
#### В функции был проведен поиск операции умножения без учета приоритета скобок
#### Цикл if несколько раз был использован без фигурных скобок
#### Было прямое использование индекса без проверки
#### Так как в коде не было ни одной проверки, создана логическая функция для этого. В ней проверяется корректность введенной строки в различных случаях
#### В функции main Исправлено непонятное название переменной str на InputExpression
### Был использован getline без проверки, нет проверки на пробелы
### Исправлено непонятное название переменной r на Result
